#!/bin/bash
# Project bootstrap — run at the start of every session.
# Two responsibilities:
#   1. Install dependencies (pip install, npm install, etc.)
#   2. Verify the app works (smoke test)
#
# If this script fails, the project is in a broken state.
# Fix it before starting any new feature work.
#
# Executor 在开发过程中按需填写。

# Example:
# pip install -r requirements.txt
# python -c "from app import app; print('App OK')"

echo "init.sh not configured yet. Fill in setup + smoke test commands."
exit 1
